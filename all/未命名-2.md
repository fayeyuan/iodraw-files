```mermaid
graph LR
    subgraph 输入
        A[政策文件文本 (中/英)]
    end
    A --> B(文本预处理)
    subgraph 中文处理
        B --> C[清洗/标准化]
        B --> D[中文分词/词性标注]
        C --> F[预处理后的文本]
        D --> F
        F --> G(信息提取)
        G --> H[中文场景]
        H --> J[提取的养老产业发展场景库]
    end
    subgraph 英文处理
        B --> C2[清洗/标准化]
        B --> E[英文分词/词性标注]
        C2 --> F2[预处理后的文本]
        E --> F2
        F2 --> G2(信息提取)
        G2 --> I[英文场景]
        I --> J
    end
    J --> K(微场景生成 (AI))
    K --> L[生成的微场景库]
    L --> M(功能描述生成 (AI))
    M --> N[功能描述与产品需求]
    N --> O(专家评审/头脑风暴)
    O --> P[细分的技术功能/产品功能需求]
    P --> Q(需求转化/优先级排序)
    Q --> R[结构化需求 (JSON/CSV)]
    R --> S[输出：待整合的技术功能库]
```